type: rpm-ostree
repos:
  - https://copr.fedorainfracloud.org/coprs/ryanabx/cosmic-epoch/repo/fedora-%OS_VERSION%/ryanabx-cosmic-epoch-fedora-%OS_VERSION%.repo
    # for pop-launcher (packaged as a copr)
  # - https://copr.fedorainfracloud.org/coprs/sweetbeans/cabos/repo/fedora-%OS_VERSION%/sweetbeans-cabos-fedora-%OS_VERSION%.repo
    # for auto-cpufreq
install:
  
  # system
  # - auto-cpufreq
  
  # editor
  - neovim
  
  # PopOS tiling window manager
  - gnome-shell-extension-pop-shell
  - pop-launcher

  # gnome extension
  - gnome-shell-extension-just-perfection

remove:
  - gnome-classic-session-xsession
  - gnome-classic-session
  - gnome-shell-extension-apps-menu
  - gnome-shell-extension-places-menu
  - gnome-shell-extension-window-list